language: python

python:
  - "3.6"
  - "3.7"

script:
  - pip install .
  - pip install -r test_requirements.txt
  - py.test

after_success:
  - if [[ ( "$TRAVIS_BRANCH" == "master" ) && ( "$TRAVIS_PULL_REQUEST" == "false" ) ]]; then
    make publish;
    fi
